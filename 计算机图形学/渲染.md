OCC的BRepMesh_IncrementalMesh在顶点阶段之前

## 渲染基础
顶点着色阶段（Vertex Shader Stage）
图元组装和几何处理（Primitive Assembly & Geometry Processing）
光栅化（Rasterization）
片段着色阶段（Fragment Shader Stage）

display -> NDC -> clip(projection) -> camera(view) -> world

见ry-s-ray项目的光线生成代码
<br><br>

## 画直线
Bresenham算法 记结果 用法就行，看什么推导过程，，，

固定一套斜率小于1的算法，斜率大于1的时候，直接交换x y的值就行
<br><br>

## 重心坐标
判断点是否在三角形内外，这种低级公式的代码，用AI写就行，巨快，且精准
<br><br>

## shadertoy
这个项目让我知道了 uv坐标的妙处
实际上渲染也可以用纹理贴图的形式来完成
<br><br>

## tiny render
这个课程没看完，就直接去写光线追踪了