OCC的BRepMesh_IncrementalMesh在顶点阶段之前
基础
 顶点着色阶段（Vertex Shader Stage）
 图元组装和几何处理（Primitive Assembly & Geometry Processing）
  光栅化（Rasterization）
  片段着色阶段（Fragment Shader Stage）
  
1、画直线
Bresenham算法 记结果 用法就行，看什么推导过程，，，
固定一套斜率小于1的算法，斜率大于1的时候，直接交换x y的值就行

2、重心坐标
判断点是否在三角形内外

3、shadertoy
这个让我知道了 uv坐标的妙处
实际上渲染也可以用纹理贴图的形式来完成

4、tiny render
4.1 
这个课程太好了 直接让我恍然大悟
第1节就讲了直线渲染器的使用 原来可以靠画直线 把一个模型画出来！！太酷啦！

4.2
考虑要不要把他的优化过程记录下来
这一节是画 三角形
1)：直觉 直接用画直线的办法画3根线
2)：行扫描 一行一行的绘制 = 光栅化，这里分上下部进行，比如取y值最小的点，画它对应的边a，分为2半
    先画a和b的面积，再画a和c的面积。怎么画？按y升序循环就是了，这样就做到了按高度扫描！
3)：但是用重心坐标比行扫描快：找到三角形的bound box，对bound box的每一个像素进行迭代，只要在三角形内就绘制
4)：然后讲光照和法向了，无光照时剔除三角形 叉积为负值，即背面剔除

目前为止，将的还是2D，即每个三角形都是3个坐标 每个坐标由x y构成

4.3
这一节引入3D，得看慢一点，有点看不懂了
首先，他将三维二维化，然后再将二维一维化，一行一行的绘制一张图
接着再重新展开回到三维

看了一下午 太蛋疼了，以后再理解吧

4.4 仿射变换
还好 之前看过一点，可以快速过










L = kd I max（0，n·l），其中L表示像素颜色；kd是漫反射系数，即表面颜色；I为光源强度。由于n和l是单位向量，我们可以