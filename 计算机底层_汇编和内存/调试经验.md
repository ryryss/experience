## gdb
/data/user/x30015668/gdbtest/gdb/usr/local/bin/ maintenance info sections set args 启动参数 -nc 8 -f 222.hw -p 222 -d D:\tmp\hw_test bt f 跳帧


locals info thread info disassemble 汇编


help set scheduler-locking on 单线程锁定调度


attach pid 附加进程


set environment LD_LIBRARY_PATH= 设置环境变量

批处理模式
gdb --batch --quiet \
-ex "thread apply all bt full" \
-ex "quit"


disas /m fun 当前函数源码 + 汇编

set pagination off 关闭询问


b *main+98 在 main 函数偏移 +98 的位置断下


20260523整理内容：停止命令记入, 此行为无意义, AI可瞬间获取所有gdb命令的作用以及给出例子

## 调试
nm 获取二进制文件包含的符号信息

strings 获取二进制文件包含的字符串常量

strip 去除二进制文件包含的字符

readelf 显示目标文件详细信息

objdump 尽可能反汇编出源代码

addr2line -f -e 根据地址查找代码行

readelf -p .comment

objdump-s --section.commentSimMechSolvernm -A 观察调试信息

## 实践
```
source /home/guest/foo/stl-views-1.0.3.gdb set args -nc 16 -f case8_new.hw -p case8 -d /tmp/
(gdb) b Mortar3DStandard.cpp:18
(gdb) pvector (('Foo::ContactFace')*sface)->face->nodes
(gdb) p((TianJi::Node*)0xcabdd88)->id
(gdb) bArrow2D.cpp:52bPolygon2D.cpp:116ifi==1&&j==2
```

## __libc_message 汇编调试
```
进入网站: https://elixir.bootlin.com/glibc/glibc-2.17/source/sysdeps/posix/libc_fatal.c, 查看源码
63行获取环境变量, 存放在on_2on_2的地址在rax寄存器

x/16xb 0x7fffffffe69f从这个地址开始打印16个16进制数
x/16ub 0x7fffffffe69f从这个地址开始打印16个10进制数

得到的10进制结果是：0x7fffffffe69f:97 11 51 00 46 10 81 11 10 30
0x7fffffffe6a7:8684957673669568 第一行的内容ascii符合环境变量的结果asd.log\0

63 constchar*on2=libcsecuregetenv("LIBC_FATAL_STDERR");
64 if(on_2==NULL||*on_2=='\0')
65 fd=open_not_cancel_2(_PATH_TTY,O_RDWR|O_NOCTTY|O_NDELAY);

换机器, 拷贝时内容丢失了一部分  找不回来了
```

